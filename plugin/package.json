{
  "name": "curl-load",
  "version": "0.1.0",
  "description": "CLI tool for curl-load — submit load tests from the command line",
  "type": "module",
  "bin": {
    "curl-load": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js"
  },
  "dependencies": {
    "commander": "^12.1.0"
  }
}