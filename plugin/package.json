{
  "name": "perfload",
  "version": "0.1.0",
  "description": "CLI tool for perfload — submit load tests from the command line",
  "type": "module",
  "bin": {
    "perfload": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js"
  },
  "dependencies": {
    "commander": "^12.1.0"
  }
}